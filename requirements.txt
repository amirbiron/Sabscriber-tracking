# Subscriber_tracking Bot - Requirements for Render Deployment
# בוט טלגרם חכם לניהול מנויים

# Core dependencies
python-telegram-bot==20.0
apscheduler==3.10.4

# Environment management
python-dotenv==1.0.0

# Web requests
requests==2.31.0

# OCR Support (זיהוי טקסט מתמונות) - Optional
pytesseract==0.3.10
Pillow==10.1.0

# Note: sqlite3 is built into Python, no need to install
